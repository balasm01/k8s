apiVersion: v1
kind: Pod
metadata:
  name: hello-pod
  labels:
    app: web
    owner: mahab
spec:
  containers:
    - name: web-ctr
      image: nigelpoulton/getting-started-k8s:1.0
      ports:
        - containerPort: 8080
